# 検証手順

第三者がこの台帳を検証する手順です。3行で終わります。

```bash
git clone --depth=1 https://github.com/mojamojamojao/sealed-ledger ledger && cd ledger
python verify_chain.py            # 全日のハッシュ連鎖と宣言レース数を再計算して照合
openssl ts -verify -data daily/20260824/races/20260824_03_01.json -in daily/20260824/races/20260824_03_01.json.tsr -CAfile "$(python -c 'import certifi, sys; sys.stdout.write(certifi.where())')"
```

- `verify_chain.py` は封印済み JSON のバイト列から sha256 を再計算し、日次マニフェスト・前日リンク・宣言レース数との一致を全日分検査します。スクリプト本体はこのリポジトリに同梱するので、隠れた処理はありません。標準ライブラリだけで動きます。
- 3行目は RFC 3161 タイムスタンプ (DigiCert TSA) の暗号検証です。例は 1 レースの封印 (この商品の中心的な主張 = 「この確率が締切前に存在した」) を検証しています。`.tsr` を持つ他のファイル (各日の `schedule.json`、クローズ済みの日の `manifest.json`) も同じ形で検証できます。**CA 証明書はこちらから配布しません** — 検証の信頼の起点をこちらが供給しては意味がないので、お手元の CA ストア (例では `certifi`、システムの `ca-certificates` でも可) を指定してください。
- **RETROSPECTIVE (過去データでの再現) の日にはファイル単位のタイムスタンプがありません。** これらは封印運用の実績ではなく、公開開始前に生成した再現なので、存在時刻は「最初の封印運用日のタイムスタンプ」から連鎖経由でしか言えません。封印運用日 (FORWARD) は全レースにタイムスタンプが付きます。
- `verify_chain.py` は失敗でないことも報告します。`UNCLOSED (no manifest)` はその日の封印ループが当日中に完了しなかった日 (封印物は残っていますが、その日の封印集合は連鎖では固定されていません)、`market binding not checkable` はその未クローズ日で市場ハッシュの比較先が無いことを意味します。どちらも検証の失敗ではなく、**欠けている証拠を欠けていると表示している**行です。該当日は台帳トップページの「連鎖に載っていない日」にも列挙しています。
- 封印時点の市場ベクトルはハッシュのみ公開しています (オッズ板の再配布可否は法務確認中のため)。ハッシュの整合性検証は誰でもでき、中身の開放は法務確認の結果に従います (確認が終わるまでは非公開です)。
- **公開している統計の出どころ**: スコアボードと較正表は、各日の `data/<日付>.json` に入っている採点結果から計算しています。採点結果自体はハッシュ連鎖の対象ではありません (連鎖が守っているのは封印です)。そこで `verify_chain.py` は、**採点に使われた確率が封印された確率と一致すること**もレース単位で照合します — 数字だけを後から良く書き換える経路を塞ぐためです。
- **温度の適用式**: 公開している確率は `p_i^T / Σ_j p_j^T` (べき乗して再正規化) です。T は各日のスケジュールと各レースの封印に含まれます。封印には採用構成 (生確率) と棄却構成 (段階PL) の両方の素の確率が入っているので、**どちらの構成の成績も公開データだけで再計算できます** (例: 再現窓 9,247 レースで棄却構成は温度適用後 1.5654、封印時点の市場が 1.2818 = −0.284 nats。これが公開前に初期構成を外した根拠です)。
- 検証に失敗する状態 (`NG:` で終了コードが 0 以外) を発見した場合、それはこのプロジェクトの終わりを意味します。再現手順つきで公開してください。
